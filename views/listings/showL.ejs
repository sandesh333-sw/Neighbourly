

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Neighborly - Community-Powered Rentals</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400..700;1,400..700&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Source+Sans+3:ital,wght@0,200..900;1,200..900&display=swap" rel="stylesheet">

</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-light shadow-sm border-bottom mb-4">
            <div class="container mt-4">
                <a class="navbar-brand " href="/">Neighborly</a>
                <a class="navbar-brand " href="/listings/legal">Browse Listings</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item"><a class="nav-link" href="/listings">Listings</a></li>
                        <li class="nav-item"><a class="nav-link" href="/groups">Groups</a></li>
                        <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
                        <li class="nav-item"><a class="btn btn-primary ms-2" href="/signup">Sign Up</a></li>
                    </ul>
                </div>
            </div>
     </header>

    <main>
        <div class="col-12 col-md-10 col-lg-8 offset-lg-2">
            <%- include('../partials/flash') %>
            <h3 class="display-5 fw-bold text-primary mb-4"><%= listing.title %></h3>
            <div class="card shadow-lg hover-shadow-xl transition-all duration-300">
                <!-- Fixed image source and added responsive image handling -->
                <img 
                    src="<%= listing.imageUrl %>" 
                    class="card-img-top img-fluid rounded-top" 
                    alt="<%= listing.title %>" 
                    style="height: 60vh; max-height: 500px; background-size: contain; object-position: center;"
                >
                
                <div class="card-body p-4 p-md-5">
                    <!-- Improved price section -->
                    <div class="d-flex justify-content-between align-items-center mb-4 bg-light p-3 rounded-3">
                        <div class="d-flex align-items-baseline gap-2">
                            <span class="text-muted fs-5">£</span>
                            <h2 class="m-0 fw-bold text-dark">
                                <%= listing.price.toLocaleString() %>
                            </h2>
                        </div>
                        <span class="text-muted fw-medium fs-5">per night</span>
                    </div>
        
                    <!-- Location section with improved icon -->
                    <div class="mb-4">
                        <p class="h5 text-secondary mb-0">
                            <i class="bi bi-geo-alt-fill me-2 text-primary"></i>
                            <%= listing.country %>
                        </p>
                    </div>
        
                    <!-- Description section with better typography -->
                    <div class="mb-5">
                        <h4 class="text-primary mb-3 fw-semibold">About this space</h4>
                        <p class="lead text-muted lh-base" style="letter-spacing: 0.5px;">
                            <%= listing.description %>
                        </p>
                    </div>
        
                    <!-- Agency section with improved styling -->
                    <div class="border-top pt-4">
                        <h4 class="text-primary mb-3 fw-semibold">Agency</h4>
                        <div class="d-flex align-items-center gap-3">
                            <i class="bi bi-building fs-2 text-primary"></i>
                            <div>
                                <p class="h5 text-dark mb-0">Pulami Properties</p>
                                <small class="text-muted">Registered since 2020</small>
                            </div>
                        </div>
                    </div>
        
                    <!-- Improved button layout with responsive spacing -->
                    <div class="mt-5 d-flex flex-column flex-md-row gap-3 align-items-stretch">
                        <a href="/listings/legal/<%= listing._id %>/edit" 
                           class="btn btn-danger px-4 py-3 fs-5 d-flex align-items-center gap-2 h-100">
                            <i class="bi bi-pencil-square fs-4"></i>
                            Edit Listing
                        </a>
                        <form method="POST" action="/listings/legal/<%= listing._id %>?_method=DELETE" class="w-100 d-flex">
                            <button type="submit" class="btn btn-dark px-4 py-3 fs-5 w-100 d-flex align-items-center gap-2 justify-content-center h-100">
                                <i class="bi bi-trash3 fs-4"></i>
                                Delete
                            </button>
                        </form>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <footer>
        <%- include('../partials/footer') %>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>



